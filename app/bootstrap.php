<?php

/**
 * Application bootstrap.
 * Include this file once at the top of every entry-point that needs OOP classes.
 * It registers the autoloader so any App\* class is lazily loaded on first use.
 */
require_once __DIR__ . '/autoload.php';
